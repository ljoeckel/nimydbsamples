In this repo you will find samples for nim-yottadb.

src/datastar
A simple form app that saves the formdata in YottaDB. Uses 'asyncHttpServer'
Run 'nimble demo' to build and run the webserver. Then open a WebBrowser at http://localhost:8080
